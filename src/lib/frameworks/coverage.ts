import type { CoverageEntry } from "./types";

/**
 * Frameworks Aptenodyte currently claims product coverage for.
 * Keep empty until ready to support items — then add entries like:
 *   { frameworkId: "soc2", status: "supported" }
 *   { frameworkId: "gdpr", status: "in-development" }
 */
export const coverageEntries: CoverageEntry[] = [];
