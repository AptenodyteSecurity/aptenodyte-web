import SectionStub from "@/components/dashboard/SectionStub";

export default function AnalyticsPage() {
  return (
    <main
      id="main-content"
      tabIndex={-1}
      className="mx-auto w-full max-w-4xl flex-1 px-6 py-8"
    >
      <SectionStub
        title="Analytics"
        description="Jurisdiction coverage and other summaries from uploaded datasets will live here. This is a UI shell for now."
      />
    </main>
  );
}
