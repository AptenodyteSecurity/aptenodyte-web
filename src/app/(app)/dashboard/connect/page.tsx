import SectionStub from "@/components/dashboard/SectionStub";

export default function ConnectDatabasePage() {
  return (
    <main
      id="main-content"
      tabIndex={-1}
      className="mx-auto w-full max-w-4xl flex-1 px-6 py-8"
    >
      <SectionStub
        title="Connect database"
        description="A future connection to an external database will land here. Nothing is wired up in this version."
      />
    </main>
  );
}
